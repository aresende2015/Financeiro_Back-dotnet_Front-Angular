/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { TaxadenegociacaoService } from './taxadenegociacao.service';

describe('Service: Taxadenegociacao', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [TaxadenegociacaoService]
    });
  });

  it('should ...', inject([TaxadenegociacaoService], (service: TaxadenegociacaoService) => {
    expect(service).toBeTruthy();
  }));
});
